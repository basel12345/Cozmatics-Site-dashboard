import { ConvertToStringPipe } from './convert-to-string.pipe';

describe('ConvertToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
